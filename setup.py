from setuptools import setup

setup(
    name="nals",
    version="0.1",
    py_modules=["exam", "exam_refactor"],  # Explicitly list your modules
    install_requires=[],  # Add dependencies if needed
)
